#include "main.h"
#include <stdio.h>
/**
 * main - Check our group code
 * @n: the number of field width
 * @fieldWidth: the field width value
 * Return: nothing
 */
int main(void)
{
	int n = 42;
	int fieldWidth = 8;

	printf("%*d\n", fieldWidth, number);

	double floatingPoint = 3.14159;
	fieldWidth = 12;

	printf("%*f\n", fieldWidth, floatingPoint);
}
